apiVersion: v1
kind: Service
metadata:
  name: ${(.values.name)}
spec:
  selector:
    app: ${(.values.name)}
  ports:
  - port:  ${(.values.ports.port)}
    targetPort: ${(.values.ports.targetport)}
